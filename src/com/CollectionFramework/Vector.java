package com.CollectionFramework;

public class Vector {
    /**
     * 🧊 What is Vector in Java?
     * Vector is a dynamic array that can grow or shrink in size. It is a part of java.util package and was introduced in Java 1.0 (legacy class), but it’s still used in some multi-threaded scenarios.

     * ✅ Core Features:
     * It stores objects (non-primitive types).
     * It is synchronized (thread-safe).
     * Maintains insertion order.
     * Can contain duplicate elements

     * Vector Synchronization	Yes (Thread-Safe)	Arraylist No (Not Thread-Safe)
     * Vector  Performance	Slower (sync overhead) Arraylist Faster
     * Vector  Legacy	Yes	 Arraylist No
     * Vector  Growth Factor	Arraylist Doubles	50%.

     * Method	        Description
     * add()	        Adds an element
     * addElement()  	Legacy method to add an element
     * remove()     	Removes element
     * size()	        Returns current size
     * capacity()	    Returns current capacity
     * elementAt(int)	Returns element at specific index
     * firstElement()	Returns first element
     * lastElement()	Returns last element
     * isEmpty()	    Checks if vector is empty
     *
     * ***/

}
